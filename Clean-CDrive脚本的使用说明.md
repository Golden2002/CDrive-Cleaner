这个脚本在**正确理解并谨慎使用的前提下是相对安全的**，但不同清理级别的风险差异很大，需要你根据自身情况决定。下面我详细分析其安全性和注意事项。

---

## 一、脚本工作原理

- 脚本通过预定义的“规则”删除指定文件夹**内部**的所有文件和子目录（保留文件夹本身）。
- 每个规则有对应的清理级别（1~4），级别越高，删除的内容越多、可能影响的应用也越多。
- 部分规则标记为 `IsDangerous = $true`，表示删除后可能导致应用数据丢失（如聊天记录、登录状态）。
- 提供预览模式 (`-Preview`) 和确认机制 (`-Confirm`)，执行前可查看预期释放的空间和具体内容。

---

## 二、各清理级别的风险分析

| 级别 | 删除内容 | 风险等级 | 说明 |
|------|----------|----------|------|
| **Level 1** | `C:\Windows\Temp`、用户临时文件夹 | ✅ 安全 | 临时文件，删除后不会影响系统或应用，可能释放少量空间。 |
| **Level 2** | npm/pip 缓存、OfficePLUS、GitHub Desktop 缓存 | ✅ 安全 | 这些都是程序缓存，删除后下次使用时重新生成，不影响数据。 |
| **Level 3** | QQ/微信数据、钉钉缓存、WPS 缓存、JetBrains IDE 缓存、Chrome 缓存、Python 本地缓存 | ⚠️ 中等风险 | - **QQ/微信数据**：包含聊天记录、接收的文件、头像等，**不可恢复**。<br>- **钉钉缓存**：删除后需重新登录，部分本地聊天记录可能丢失。<br>- **Chrome 缓存**：仅影响浏览器速度，不会丢失书签/密码。 |
| **Level 4** | 0install、NVIDIA、Adobe、百度网盘、夸克网盘、网易软件缓存 | ⚠️ 较低风险 | 多为软件缓存或下载临时文件，但 Adobe 可能包含正在编辑的项目缓存（未保存的内容可能丢失），网盘缓存删除后可能需重新下载部分文件。 |

> **注意**：Level 3 中标记为 `IsDangerous` 的规则（如 Tencent Files）**必须备份相关数据**，否则一旦删除，数据永久丢失。

---

## 三、脚本自身的可靠性与隐患

### ✅ 优点
- **非破坏性设计**：只删除指定路径下的内容，不删除系统文件或注册表。
- **路径检查**：`Test-Path` 确保文件夹存在才操作，避免错误。
- **错误处理**：`-ErrorAction SilentlyContinue` 避免因权限不足中断脚本。
- **预览与确认**：提供预览功能，危险项目会高亮并二次确认。

### ⚠️ 潜在问题
1. **环境变量依赖**  
   脚本使用了 `$env:TEMP`、`$env:APPDATA` 等变量，如果这些变量被修改或指向特殊路径（例如被重定向到其他盘），可能误删非预期文件。多数用户默认路径安全。

2. **强制删除**  
   `-Force` 参数会尝试删除只读文件，如果文件正在使用，会失败但不会影响系统。不过某些系统临时文件可能被占用，导致删除失败（无大碍）。

3. **危险规则的警告不够强**  
   虽然脚本会在执行前提示，但用户若习惯性按 `y` 确认，仍可能误删重要数据。建议手动备份。

4. **无撤销功能**  
   删除操作不可逆，除非你有系统还原点或文件历史备份。

---

## 四、安全使用建议

1. **先预览再执行**  
   ```powershell
   .\Clean-CDrive.ps1 -Level 3 -Preview
   ```
   查看每个规则对应的文件夹大小和警告，确认无误后再执行。

2. **备份重要数据**  
   若 Level ≥ 3，务必备份：
   - QQ/微信的聊天记录（可通过软件自带功能导出）
   - 钉钉的本地聊天记录（一般云端有，但以防万一）
   - Adobe 正在进行的项目文件

3. **逐步清理**  
   初次使用建议从 Level 1 开始，观察效果，再逐步升级，避免一次性删除过多。

4. **以管理员身份运行**  
   虽然脚本主要操作用户文件夹，但清理 `C:\Windows\Temp` 可能需要管理员权限。右击 PowerShell 选择“以管理员身份运行”。

5. **定期执行**  
   临时文件和缓存会不断增长，可每月执行 Level 1~2 保持系统整洁。

---

## 五、结论

- **Level 1~2**：**非常安全**，可以放心使用，不会影响任何应用数据。
- **Level 3**：**有一定风险**，务必确认自己清楚每个危险项的含义，并提前备份重要数据。
- **Level 4**：**风险较低**，但仍建议预览确认，避免误删有用的缓存（如 Adobe 项目缓存）。

总体而言，这个脚本是**设计良好、相对安全**的 C 盘清理工具，只要用户不盲目确认，结合预览和备份，就能安全释放磁盘空间。如果你对脚本中的任何路径不确定，可以在执行前手动打开文件夹查看内容，决定是否保留。



# 脚本使用

## **清理脚本：**

**.\Clean-CDrive2.ps1**

## **主要功能改进：**

| 功能       | 说明                                     |
| :----------: | :---------------------------------------: |
| 交互式选择 | Level 3 及以上可输入数字选择要清理的项目 |
| 按类别分组 | 开发工具、浏览器、办公软件、通讯软件等    |
| 危险项警告 | QQ/微信/钉钉等标记为危险，清理前提示     |
| 灵活选择   | 支持输入数字、输入'a'全选、输入'd'仅默认 |

## **使用方法：**

| 命令               | 结果                        |
| ------------------ | --------------------------- |
| -Level  0          | ✅ 显示各级别预估空间        |
| -Level  2 -Preview | ✅ 显示清理计划预览          |
| -Level  2 -Confirm | ✅ 执行Level 2清理（安全项） |
| -Level  3          | ✅ 检测到危险项并警告        |

```powershell
# 查看可用清理级别
.\Clean-CDrive.ps1

# 仅检测各级别可释放空间
.\Clean-CDrive2.ps1 -Level 0

# 预览清理计划
.\Clean-CDrive2.ps1 -Level 2 -Preview

# 执行Level 2（自动跳过危险项）
.\Clean-CDrive2.ps1 -Level 2 -Confirm

# 执行Level 3（会提示危险项，按y继续）
.\Clean-CDrive2.ps1 -Level 3

# 执行Level 3（跳过所有危险项）
.\Clean-CDrive2.ps1 -Level 3 -Confirm
```

**清理级别**

- Level 0: 仅检测（显示各级别预计释放空间）

- Level 1: 临时文件

- Level 2: + npm/pip/OfficePLUS/GitHub缓存

- Level 3: + 腾讯/钉钉/JetBrains/Chrome等大型应用

- Level 4: + 所有可选应用缓存


**添加新规则示例**

```bash
# 在脚本中添加新规则（新增Category参数）
# 基础用法
Add-CleanRule -Name "新应用缓存" `
    -Description "应用描述" `
    -Paths @("C:\路径1","C:\路径2") `
    -Level 2 `
    -Category "开发工具"
# 危险项用法
Add-CleanRule -Name "新应用缓存" `
    -Description "包含重要数据" `
    -Paths @("C:\路径") `
    -Level 3 `
    -IsDangerous $true `
    -Warning "清理前请备份!" `
    -Category "其他应用"
```

**新版本新增参数**

| 参数     | 说明     | 可选值                                               |
| -------- | -------- | ---------------------------------------------------- |
| Category | 分类名称 | System,  DevTools, Browser, Office, IM, Apps, Driver |

## **当前已支持的清理项**
新脚本包含更丰富的清理项，按类别分组：

- System: Windows Temp, User Temp
- DevTools: npm, pip, GitHub Desktop, JetBrains, VS Code, Python
- Browser: Chrome, Edge, Firefox
- Office: OfficePLUS, Kingsoft
- IM: QQ, WeChat, DingTalk (危险)
- Apps: iFlow, Adobe, Baidu, Quark, Netease
- Driver: NVIDIA
